package com.airfranceklm.amt.sidecar.model;

/**
 * Whether the element will be visible only in filters, or it should appear in the input. Used in the code, and not used
 * directly.
 */
public enum ElementVisibility {
    Filters, Input
}
